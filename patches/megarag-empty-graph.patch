diff --git a/megarag/kg/nano_mm_vector_db_impl.py b/megarag/kg/nano_mm_vector_db_impl.py
index 079bfbb..ede1b26 100644
--- a/megarag/kg/nano_mm_vector_db_impl.py
+++ b/megarag/kg/nano_mm_vector_db_impl.py
@@ -1,7 +1,7 @@
 import time
 import os
+import json
 import PIL
-import asyncio
 import numpy as np
 import pipmaster as pm
 
@@ -20,6 +20,7 @@ from nano_vectordb import NanoVectorDB
 from nano_vectordb.dbs import (
     DataBase,
     load_storage,
+    array_to_buffer_string,
     ConditionLambda,
     f_METRICS,
 )
@@ -64,6 +65,24 @@ class DotProductVectorDB(NanoVectorDB):
 
         logger.info(f"Init {asdict(self)} {len(self._NanoVectorDB__storage['data'])} data")
 
+    def save(self):
+        storage = {
+            **self._NanoVectorDB__storage,
+            "matrix": array_to_buffer_string(self._NanoVectorDB__storage["matrix"]),
+        }
+        tmp_path = f"{self.storage_file}.tmp"
+        try:
+            with open(tmp_path, "w", encoding="utf-8") as handle:
+                json.dump(storage, handle, ensure_ascii=False)
+                handle.flush()
+                os.fsync(handle.fileno())
+            os.replace(tmp_path, self.storage_file)
+        finally:
+            try:
+                os.remove(tmp_path)
+            except FileNotFoundError:
+                pass
+
     # --- Dot-product query implementation ---
     def _dot_query(
         self,
@@ -157,6 +176,7 @@ class NanoMMVectorDBStorage(NanoVectorDBStorage):
         if not data:
             return
 
+        items = list(data.items())
         current_time = int(time.time())
         list_data = [
             {
@@ -164,47 +184,87 @@ class NanoMMVectorDBStorage(NanoVectorDBStorage):
                 "__created_at__": current_time,
                 **{k1: v1 for k1, v1 in v.items() if k1 in self.meta_fields},
             }
-            for k, v in data.items()
+            for k, v in items
         ]
-        keys = list(list(data.values())[0].keys())
-        contents, page_imgs, fig_imgs = [], [], []
+        keys = list(items[0][1].keys())
+        contents, page_image_paths = [], []
         if "content" in keys:
-            contents  = [v["content"] for v in data.values()]
+            contents = [v["content"] for _, v in items]
         if "page_img" in keys:
-            page_imgs = [Image.open(v["page_img"]).convert("RGB") for v in data.values()]
-        if "fig_imgs" in keys:
-            fig_imgs = [v["fig_imgs"] for v in data.values()]
-
-        if len(page_imgs) > 0:
-            batches = [
-                {
-                    "images":  page_imgs[i : i + self._max_batch_size],
-                }
-                for i in range(0, len(contents), self._max_batch_size)
-            ]
-        else:
-            batches = [
-                {
-                    "texts" : contents[i : i + self._max_batch_size],
-                }
-                for i in range(0, len(contents), self._max_batch_size)
-            ]
-        # Execute embedding outside of lock to avoid long lock times
-        embedding_tasks = [self.embedding_func(**batch) for batch in batches]
-        embeddings_list = await asyncio.gather(*embedding_tasks)
-
-        embeddings = np.concatenate(embeddings_list)
-        if len(embeddings) == len(list_data):
-            for i, d in enumerate(list_data):
-                d["__vector__"] = embeddings[i]
-            client = await self._get_client()
-            results = client.upsert(datas=list_data)
-            return results
-        else:
-            # sometimes the embedding is not returned correctly. just log it.
-            logger.error(
-                f"embedding is not 1-1 with data, {len(embeddings)} != {len(list_data)}"
+            page_image_paths = [v["page_img"] for _, v in items]
+
+        client = await self._get_client()
+        existing_ids = {
+            record["__id__"]
+            for record in client.get([record["__id__"] for record in list_data])
+        }
+        pending_indices = [
+            index
+            for index, record in enumerate(list_data)
+            if record["__id__"] not in existing_ids
+        ]
+        if not pending_indices:
+            logger.info(
+                f"All {len(list_data)} embeddings already checkpointed in {self.namespace}"
             )
+            return {"update": [], "insert": [], "skipped": list(existing_ids)}
+
+        checkpoint_interval = max(
+            1,
+            int(
+                self.global_config.get("addon_params", {}).get(
+                    "embedding_checkpoint_interval", 16
+                )
+            ),
+        )
+        results = {"update": [], "insert": [], "skipped": list(existing_ids)}
+        since_checkpoint = 0
+
+        # Decode only the active batch and await it before opening the next one.
+        # Full manuals otherwise retain every RGB page while all embedding calls
+        # queue at once, which can exhaust unified memory and swap.
+        for offset in range(0, len(pending_indices), self._max_batch_size):
+            indices = pending_indices[offset : offset + self._max_batch_size]
+            if page_image_paths:
+                images = []
+                try:
+                    for index in indices:
+                        path = page_image_paths[index]
+                        with Image.open(path) as image:
+                            images.append(image.convert("RGB"))
+                    embeddings = await self.embedding_func(images=images)
+                finally:
+                    for image in images:
+                        image.close()
+            else:
+                embeddings = await self.embedding_func(
+                    texts=[contents[index] for index in indices]
+                )
+            if len(embeddings) != len(indices):
+                raise RuntimeError(
+                    f"embedding is not 1-1 with data, {len(embeddings)} != {len(indices)}"
+                )
+
+            batch_data = []
+            for index, embedding in zip(indices, embeddings):
+                batch_data.append({**list_data[index], "__vector__": embedding})
+            batch_results = client.upsert(datas=batch_data)
+            for action in ("update", "insert"):
+                results[action].extend(batch_results[action])
+
+            since_checkpoint += len(indices)
+            processed = len(existing_ids) + offset + len(indices)
+            if since_checkpoint >= checkpoint_interval or processed == len(list_data):
+                if not await self.index_done_callback():
+                    raise RuntimeError(
+                        f"failed to checkpoint {self.namespace} after {processed} embeddings"
+                    )
+                logger.info(
+                    f"Checkpointed {processed}/{len(list_data)} embeddings in {self.namespace}"
+                )
+                since_checkpoint = 0
+
+        return results
 
     async def query(
         self, query: str, top_k: int, ids: list[str] | None = None
diff --git a/megarag/megarag.py b/megarag/megarag.py
index 57938fe..7ca6434 100644
--- a/megarag/megarag.py
+++ b/megarag/megarag.py
@@ -667,6 +667,7 @@ class MegaRAG(LightRAG):
                                                 }
                                             }
                                         )
+                                        raise
 
                                     # After extraction succeeds, perform merge and optional refinement
                                     if file_extraction_stage_ok:
@@ -869,6 +870,7 @@ class MegaRAG(LightRAG):
                                                 }
                                             }
                                         )
+                                        raise
 
                             except Exception as e:
                                 # Log error and update pipeline status
@@ -913,6 +915,7 @@ class MegaRAG(LightRAG):
                                         }
                                     }
                                 )
+                                raise
 
                         # Create processing tasks for all documents
                         doc_tasks = []
diff --git a/megarag/operate.py b/megarag/operate.py
index ec7ce82..fd5c172 100644
--- a/megarag/operate.py
+++ b/megarag/operate.py
@@ -1239,6 +1239,12 @@ async def merge_nodes_and_edges(
     for edge_key, edges in all_edges.items():
         tasks.append(asyncio.create_task(_locked_process_edges(edge_key, edges)))
 
+    if not tasks:
+        logger.warning(
+            "No entities or relationships were extracted; skipping the graph merge"
+        )
+        return
+
     # Execute all tasks in parallel with semaphore control and early failure detection
     done, pending = await asyncio.wait(tasks, return_when=asyncio.FIRST_EXCEPTION)
 
@@ -1528,12 +1534,19 @@ async def extract_entities_refinement(
     llm_response_cache: BaseKVStorage | None = None,
     text_chunks_storage: BaseKVStorage | None = None,
 ) -> list:
-    chunk_results_at_stage_one = {
-        list(res[0].values())[0][0]['source_id']: {
-            'nodes': res[0],
-            'edges': res[1],
-        } for res in chunk_results
-    }
+    chunk_results_at_stage_one = {}
+    for nodes, edges in chunk_results:
+        source_ids = {
+            item["source_id"]
+            for records in [*nodes.values(), *edges.values()]
+            for item in records
+            if item.get("source_id")
+        }
+        for source_id in source_ids:
+            chunk_results_at_stage_one[source_id] = {
+                "nodes": nodes,
+                "edges": edges,
+            }
 
     use_llm_func: callable = global_config["llm_model_func"]
     entity_extract_max_gleaning = global_config["addon_params"]["entity_extract_max_gleaning"]
@@ -1812,7 +1825,10 @@ async def extract_entities_refinement(
 
     tasks = []
     for c in ordered_chunks:
-        task = asyncio.create_task(_process_with_semaphore(c, chunk_results_at_stage_one[c[0]]))
+        stage_one = chunk_results_at_stage_one.get(
+            c[0], {"nodes": {}, "edges": {}}
+        )
+        task = asyncio.create_task(_process_with_semaphore(c, stage_one))
         tasks.append(task)
 
     # Wait for tasks to complete or for the first exception to occur

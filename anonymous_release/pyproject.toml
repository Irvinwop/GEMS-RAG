[build-system]
requires = ["setuptools>=69"]
build-backend = "setuptools.build_meta"

[project]
name = "mutcd-rag-comparison"
version = "1.0.0"
description = "Retrieval pipelines for the anonymous MUTCD RAG comparison study."
requires-python = ">=3.11,<3.14"
dependencies = []

[tool.setuptools.packages.find]
where = ["src"]
